<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Define Solid Color Brushes for the Dark Theme -->
    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#FFE9E9E9"/>
    <SolidColorBrush x:Key="TextBrush" Color="#FF1F1F1F"/>
    <SolidColorBrush x:Key="ButtonBackgroundBrush" Color="#FFD3281B"/>
    <SolidColorBrush x:Key="WindowHeaderBrush" Color="#FFDA291C"/>
    <SolidColorBrush x:Key="BorderBrush" Color="#FFE0E0E0"/>
    <SolidColorBrush x:Key="ContentBrush" Color="white"/>
    <SolidColorBrush x:Key="ButtonHighlightBrush" Color="#FFDA291C"/>
    <SolidColorBrush x:Key="AltTextColor" Color="White"/>

    <!-- Button Style -->
    <Style TargetType="Button">
        <Setter Property="Background" Value="{StaticResource ButtonBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
        <Setter Property="BorderThickness" Value="2" />
        <Setter Property="Padding" Value="8" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Margin" Value="2"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}" 
                            CornerRadius="4">
                        <Grid>
                            <Image Source="{Binding Tag, RelativeSource={RelativeSource AncestorType=Button}}" 
                                   Width="40" Height="40" 
                                   VerticalAlignment="Center" HorizontalAlignment="Center" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource BorderBrush}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{StaticResource ButtonHighlightBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>